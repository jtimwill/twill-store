<div class = "col-md-offset-4" >
  <ul class="pagination">
    <% if @pages > 1 %>
      <% if @current_page == 1 %>
        <li><%= link_to "«", "" %></li>
      <% else %>
        <li><%= link_to "«", category_path(offset: Product::PER_PAGE*(@current_page - 2), id: @category.id) %></li>
      <% end %>

      <% @pages.times do |index| %>
        <% if @current_page - 1 == index %>
          <li class="disabled"><a href="#"><%= "#{index+1}" %></a></li>
        <% else %>
          <li><%= link_to "#{index+1}", category_path(offset: index*Product::PER_PAGE, id: @category.id) %></li>
        <% end %>
      <% end %>

      <% if @current_page == @pages %>
        <li><%= link_to "»", "" %></li>
      <% else %>
        <li><%= link_to "»", category_path(offset: Product::PER_PAGE*(@current_page), id: @category.id) %></li>
      <% end %>
    <% end %>
  </ul>
</div >
